package linklist.search;

public class findMidElement {

/*
    static Node findMiddleNode(Node input){
        //Base case
        if (input == null)
            return input;
        Node slow = input;
        Node fast = input.getNextNode();
        while(fast!= null){
            fast = fast.getNextNode();
            if(fast!=null){
                fast = fast.getNextNode();
                slow = slow.getNextNode();
            }
        }
        return slow;
    }
    */


}
